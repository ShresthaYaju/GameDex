<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="GameDex.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:GameDex"
    xmlns:pages="clr-namespace:GameDex.Pages"
    Shell.FlyoutBehavior="Disabled">

    <!-- Tabs
    <TabBar x:Name="AppTabs">
        <Tab Title="LandingPage">
            <ShellContent ContentTemplate="{DataTemplate pages:LandingPage}" />
        </Tab>
        <Tab Title="AddPage">
            <ShellContent ContentTemplate="{DataTemplate pages:AddPage}" />
        </Tab>
    </TabBar> 
       -->
        
    <ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate pages:LandingPage}"
        Route="MainPage" />

</Shell>
